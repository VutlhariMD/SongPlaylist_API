# Song REST API

## Project Overview

The Song REST API is a backend application built using **Node.js** and **TypeScript**.

The API allows users to manage a collection of songs. Users can add, view, update, and delete songs using HTTP requests.

The API can be tested using **Postman**.\
--------

